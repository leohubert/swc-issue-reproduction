{
  "scripts": {
    "test-work": "ts-node --transpile-only src/index-work.ts",
    "test-broken": "ts-node --transpile-only src/index-broken.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.1.2",
    "typescript": "^4.9.4",
    "swc": "^1.0.11"
  }
}
